AI_PROVIDER=auto

# Optional: set one provider key to enable real AI summaries.
# With no key, the backend returns deterministic offline summaries.
GEMINI_API_KEY=
OPENAI_API_KEY=

# Optional model overrides.
GEMINI_MODEL=gemini-2.0-flash
OPENAI_MODEL=gpt-4o-mini
